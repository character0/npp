package com.pabslabs.core

//import org.apache.commons.collections.buffer.CircularFifoBuffer

/**
 * Created with IntelliJ IDEA.
 * User: root
 * Date: 5/8/13
 * Time: 2:59 PM
 * To change this template use File | Settings | File Templates.
 */
class SynchronizedRingBuffer {

   /* private CircularFifoBuffer ringBuffer

    SynchronizedRingBuffer() {
        ringBuffer = new CircularFifoBuffer()
    }

    SynchronizedRingBuffer(int size) {
        ringBuffer = new CircularFifoBuffer(size)
    }

    synchronized boolean add(element) {
        return ringBuffer.add(element)
    }

    synchronized Object get() {
        return ringBuffer.get() 
    }

    synchronized boolean isFull() {
        return ringBuffer.full 
    }

    CircularFifoBuffer getRingBuffer() {
        return ringBuffer
    }*/

}
